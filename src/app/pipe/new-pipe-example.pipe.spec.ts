import { NewPipeExamplePipe } from './new-pipe-example.pipe';

describe('NewPipeExamplePipe', () => {
  it('create an instance', () => {
    const pipe = new NewPipeExamplePipe();
    expect(pipe).toBeTruthy();
  });
});
